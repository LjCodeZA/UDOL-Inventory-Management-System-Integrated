@model IEnumerable<InventoryManagementSystem.Models.ProductStock>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductVendor.ProductVendorId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StockTakeDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductVendor.ProductVendorId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StockTakeDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductStockId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductStockId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductStockId })
        </td>
    </tr>
}

</table>
